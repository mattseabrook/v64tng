// views.cpp — Default view registration (no-op).
//
// Projects override this file (via source-key priority) to populate
// the view map and return a starting view for direct-to-game mode.

#include "game.h"

std::string buildViewMap() { return ""; }
